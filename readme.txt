1)go mod init <module name>
2)go get github.com/gin-gonic/gin
3)import (
	"net/http"

	"github.com/gin-gonic/gin"
)

use the first two steps in order to get all the packages and dependencies. 
The third step is to use the packages in code.